import DocsContent from "../../../components/DocsContent";

import content from "../../../static/docs/api reference/Build.md";

const Build = props => {
  return <DocsContent subsection="Build" content={content} />;
};

export default Build;
